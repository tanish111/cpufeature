[package]
name = "cpufeature"
version = "0.1.0"
edition = "2024"
description = "A no_std library for querying x86/x86-64 CPU features via CPUID."
license = "MIT"
readme = "README.md"
categories = ["no-std", "hardware-support"]
keywords = ["cpuid", "x86", "cpu", "feature", "no_std"]

[dependencies]
